[aliases]
test=pytest
release = dists upload
dists = clean --all sdist bdist_wheel

[tool:pytest]
norecursedirs=dist build .tox .eggs
addopts = --cov=torchex
python_files = test/*
minversion = 3.5

[bdist_wheel]
universal = 1
